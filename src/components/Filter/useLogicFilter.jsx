function useLogicFilter() {
    //START статический контент
    const arrDocument = ['В порядке', 'Нет или проблемные'];
    const arrDamage = ['Не требуется ремонт', 'требуется ремонт или не на ходу'];

    return {arrDocument , arrDamage}
}